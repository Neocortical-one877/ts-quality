---
summary: "Bridge-layer overview for mapping ontology concepts to repo artifacts."
read_when:
  - "When editing ontology-to-code mappings"
  - "When checking what belongs in the bridge layer"
type: "reference"
---

# Bridge

The bridge layer links ontology concepts to repo artifacts (code symbols, APIs, schemas).

- `mapping.yaml` maps stable concept IDs → repo-local symbols/paths.

