---
summary: "Guidance for adding repo-local ontology concepts only when truly needed."
read_when:
  - "When proposing a new repo-local concept"
  - "When deciding whether a concept belongs locally or in a broader overlay"
type: "reference"
---

# Repo Concepts

Add repo-local concepts here only when needed.

Guideline: if a concept is useful across many repos, propose it in the company overlay (or kernel), not here.

